# Vigil
Automated API testing tool. Upload an OpenAPI spec, and Vigil generates requests, validates responses against the schema, and logs results to Postgres with an HTML report. Built with FastAPI, SQLAlchemy, and Docker.
